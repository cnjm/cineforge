# ============================================================
# CineForge h5 · 环境变量模板
# 复制为 .env.development / .env.production 使用
# 无需提交库；域名/地址均为配置项，请按实际替换
# ============================================================

# 后端 API 基址（Go 单体，处理 CORS）
# 生产：OSS 静态托管 + 独立 API 域名 → 填正式域名
# 本地：留空 = 同源 /api（走 vite proxy，见 vite.config.ts）
# 正式域名：cine-forge-server.cnjm.top
VITE_API_BASE=https://cine-forge-server.cnjm.top

# Go 网关（P5 画布/编排并入后使用；当前可留空）
# VITE_GO_API_BASE=https://cine-forge-server.cnjm.top

# TapCanvas 兼容层（旧 tap-api；新架构未定，留空 = 同源 /tapapi）
# VITE_TAPAPI_BASE=

# 画布项目嵌入地址（React-Flow 画布）
# VITE_CANVAS_URL=http://localhost:5174

# ---- 仅本地 dev 生效（vite proxy 目标）----
# VITE_CINEFORGE_API_PROXY=http://localhost:8000
# VITE_TAPAPI_PROXY=http://localhost:8788