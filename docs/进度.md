# CineForge · 实施进度

> 更新：2026-09-03（P3、P4 全部完成）

## 阶段总览

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0 | 骨架：根入口 + 两仓库 init + AGENTS/CLAUDE + skill | ✅ 完成 |
| P1 | h5 前端工程化落地（样式不变，env/API 改造，删 nginx） | ✅ 完成 |
| P2 | server 骨架：单库 schema + goose + gin 空跑 + dev compose | ✅ 完成 |
| P3 | Go 业务域落地（登录/项目/拆解/任务/资产/通知/看板） | ✅ 完成 |
| P4 | agent 接入（Go→agent→Hermes 五步拆解 + 前端硬契约） | ✅ 完成 |
| P5 | 画布与编排并入 | ⏳ 未开始 |
| P6 | 云效 CICD + deploy.sh + docs 收尾 | ⏳ 未开始 |

## P0 详细清单

- [x] 根目录 README.md / AGENTS.md / docs/
- [x] 方案文档归档进 docs/开发总方案.md
- [x] cineforge-h5 仓库初始化 + CLAUDE.md / AGENTS.md
- [x] cineforge-server 仓库初始化 + CLAUDE.md / AGENTS.md / agent/
- [x] 全局 skill（~/.claude/skills/cineforge-*）

## P1 完成记录（2026-09-01）

- [x] 前端全量源码落地（119 个 src 文件 + public/config，样式未动），API 寻址 env 化、删 nginx
- [x] API 寻址 env 化：`api.ts` 新增 `API_BASE`，SSE（`useCineForgeApp`）、媒体读取（Assets/AssetLibrary/review）全部前缀化
- [x] `tapApi.ts` 新增 `TAPAPI_BASE`；`goApi.ts` 保留 `VITE_GO_API_BASE`
- [x] `vite.config.ts`：`__CANVAS_URL__` 改 `VITE_CANVAS_URL` 注入，proxy 默认指向 localhost，支持 env 覆盖
- [x] 品牌文案统一为 CineForge（login/AppShell/审片台/AgentAdmin 等 8 处）；前端 storage key 沿用既有命名（不随品牌文案调整）
- [x] `.env.example / .env.development / .env.production` 就位（域名占位）
- [x] 删除 `nginx.conf` / `tap-web-nginx*.conf` / `Dockerfile`（OSS 托管）
- [x] 验证：`npm test` 66/66 通过；`npm run build` 通过
- [x] 品牌素材：前端 logo 已改用 `public/cineforge-logo.png` / `public/cineforge-mark.png`（legacy 图片已移除）

## P2 完成记录（2026-09-01）

- [x] 后端骨架：gin 路由（/health /ready）、config 双通道（config.yaml + CINEFORGE_* env）、goose migrate CLI、dev compose 底座（PG16+pgvector / Redis7 / MinIO）
- [x] 单库 schema 映射定稿（docs/后端/单库schema映射.md）：四项决策全部确认（users 唯一主 / outbox 移除 / 画布前缀改名）
- [x] 生成 `cineforge-server/migrations/00001_initial_single_db_schema.sql`（46 业务表 + goose_db_version）：按定稿 schema 构建 → pg_dump --no-owner 实测导出，非手抄
- [x] End-to-end 验证：空库 `goose up` → 48 表（46 业务 + goose 版本表）表名与定稿清单逐一对齐；`down` 干净回滚到 0；重 `up` 恢复；`/ready` 200
- [x] 修复：goose 迁移中剥离 pg_dump 的 `\restrict`/`set_config search_path` 等 psql 元命令壳
- [ ] 待 P3：Go repository/model 层按 46 表落地

## P3c 完成记录：auth 域（2026-09-01）

- [x] 迁移 `00002_auth_casbin_baseline.sql`：5 个平台角色 + Casbin 能力种子（projects.read / tasks.read / flows.write / tasks.submit 全员，tasks.review 仅 admin/director），已在 postgres 验证
- [x] `internal/config` 新增 auth 块 + env 双通道（CINEFORGE_AUTH_SECRET / CINEFORGE_AUTH_SESSION_TTL_SECONDS / CINEFORGE_TAPCANVAS_JWT_SECRET / CINEFORGE_PLATFORM_JWT_PRIVATE_KEY / CINEFORGE_PLATFORM_JWT_KID）
- [x] `internal/auth/password.go`：PBKDF2-HMAC-SHA256 120k 轮（`pbkdf2_sha256${salt-hex}${digest-hex}`），默认密码=手机号后 6 位；Python 互操作向量通过
- [x] `internal/auth/tokens.go`：三 token 签发/校验（access 2-part HMAC / tapcanvas HS256 / platform RS256），TTL 上限 8h，共享 session_expires_at；Python access token 互操作向量通过
- [x] `internal/casbin`：基于 sys_casbin_rule 种子的 enforcer（60s 缓存）
- [x] repo/service/handlers：POST /api/auth/login|me|verify-password|password、GET/POST /api/users、PATCH /api/users/:id/status、POST /api/users/:id/reset-password；detail 文案与既定契约逐字透传
- [x] 契约测试全绿：`go test ./...`（auth / casbin / api 三包）；规范化契约见 docs/后端/auth契约.md
- [ ] 待办：admin 批量建号 + 员工管理页联调（前端）随 P3g 覆盖（✅ P3g 完成：批量建号为前端循环调用 POST /users，已由 P3c 后端支持）

## P3d 完成记录：项目域（2026-09-01）

- [x] repository：项目/分集/剧本版本 CRUD、`LatestScopedBreakdown`、`GetArtifactRevisionByID`、`PendingAgentRun`（input 顶层层级匹配）、`FindWorkflowByIdempotencyKey`、`UpdateWorkflowRunAgentRun`、脚本段视图、operation log
- [x] 剧本文件解析（`internal/service/script_parse.go`）：扩展名/大小校验、UTF-8/BOM→GB18030、**docx Go 原生 zip+XML 解析**（安全限制）、rune 计数控制字符校验（修复 CJK 误判）、单集标题解析
- [x] Import/POST 全流程：校验顺序、项目建档/合并、episode/script/version 落库、归档（**生产归档器恒失败 → 503**，P3f 接入 MinIO）
- [x] 五步拆解 submit（`SubmitBreakdownStep`）：步骤映射 + 作用域解析 + 前置闸口（文案逐字）+ 幂等 + `workflow_runs/agent_runs` 落库（queued）+ 操作日志；**执行留 P4**
- [x] 拆解草稿：GET current（200 或 null，读侧回填正式 reading）、POST save（乐观锁 409 + 显式人工确认 → agent/system/human 三级 reading_report revision）、确认后闸口 409
- [x] Dashboard 项目健康度聚合（storyboard/asset/task/completed/overdue/completion_rate）；缓存键约定，Redis 于 P3g 接入
- [x] `GET /api/production-brief/catalog`、`GET /api/agent-runs`、`/api/agent-runs/{id}`、`/projects/{id}/script-segments`
- [x] 契约测试全绿：`internal/api/project_contract_test.go`（import 校验/503、CRUD、episode 状态、breakdown 202+闸口、乐观锁 409、dashboard、catalog、agent-runs）；`go build / vet / test ./...` 通过
- [ ] 待办：P3f assets/files/archiver（消除 import 503 + asset 绑定 + **asset-tasks/storyboard-tasks 生成器**）、P3g notify/admin
- 契约：docs/后端/项目域契约.md

## P3e 完成记录：任务域（2026-09-02）

- [x] repository：`tasks_read.go`（TaskView 56 字段、列表/详情/审核工作台可见性）、`tasks_prompt.go`、`tasks_batch.go`（submit/review/bulk + uuid5 request_id 幂等）、`tasks_grouping.go`（storyboards / board / scene-gating / storyboard-video-tasks / bulk-assign）、`tasks_submission.go`（候选登记/编辑/软删/归档/主母版）
- [x] service 层：`TaskService` + `StatusError` 映射（404/403/400 逐字）、workspace 统计、分组读写；事务 owner（`withTx`）
- [x] handlers 路由：`/tasks` 15 条、`/workspace` 2 条、分组 `/projects/{id}` 5 条（task-assignments 走 director/admin middleware）；request_id 四消费点 UUID 硬校验（非 UUID → 422，对齐既定契约）
- [x] **权限模型补齐（决策）**：分组读沿用既定契约 `_can_access_project`（含「可见任务」回退）——`GetProjectForTask` / `canRead` / `CanAccessProject` 三处一致，修复非成员执行人 403 的偏差；`GetProductionBoard`/`SceneGating` 内部防御门从 `HasProjectPermission` 升为 `CanAccessProject`；顺带修复 `visibleStoryboardIDsForProject` 占位符冲突（$1 复用）潜在 500
- [x] 通知：改派/审核定版通知在事务内落 `notifications`（`is_read=false` 显式写，对齐既定契约）；顺带修复 seed 用户清理的 FK
- [x] 契约测试全绿：`internal/api/task_contract_test.go` 11 个用例（含 visible-task fallback 新覆盖）；`go build / vet / test ./...` 通过
- [ ] **生成器端点** `POST /projects/{id}/asset-tasks` 与 `POST /projects/{id}/storyboard-tasks` 经确认推迟到 P3f（与 asset 域同批落地）
- 契约：docs/后端/任务域契约.md

## P3f 完成记录：文件 / 资产 / 生成器 / 分集删除清理（2026-09-03）

> 关键决策（与负责人确认）：**真实 MinIO（minio-go SDK）+ 服务端流式代理**（无预签名 URL，
> Range/206/416 与参考实现逐字节对齐）；分集删除 `delete_files=true` 真正删对象；回应保持一致，前端样式 100% 不变。
> 契约见 [docs/后端/文件与资产域契约.md](后端/文件与资产域契约.md)。

- **P3f-a 存储域**：`internal/storage`（minio-go：Put/Stat/Open/Remove/EnsureBucket）+ 真实 `NewMinioArchiver` 归档器（扩展名白名单、对象键 `{prefix}/script/imports/...`、`file_code {PREFIX}-SCRIPT-{uuid8}`）——**消除 import 503**；MinIO 未配置保持 503 兜底契约
- **P3f-b 文件域**：`/files/task-upload`（multipart 手动解析保 part Content-Type、流式上交、对象键 V{n} 布点、`minio://` url）；content/stream/download/playback-url/download-url（**HMAC 文件 token 字节兼容** `file-playback:` 前缀，Range 200/206/416 + `Content-Disposition filename*=UTF-8''`，`is_company_asset_library_file` 公司资产库兜底）
- **P3f-c 资产域**：`/assets` CRUD（正式码分配、project_id 校验路径）、`/assets/library`（include_versions）、variant-plans（创建恒 400 契约）、`/assets/temporary-production`（request_id 幂等 + 未确认 400 + 跨版本守卫）、`/assets/{id}` 详情、`scene-options`（human 终版修正）、分集详情 asset_bindings、AssetVersion 物化接线
- **P3f-d 任务生成器**：`POST /projects/{id}/asset-tasks` 与 `POST /projects/{id}/storyboard-tasks`（query 必填 `episode_id`+`script_version_id`；数据链守卫逐字对齐既定契约——含 tier-1 `assets_not_confirmed` 短路、缺 Prompt Skill 409、无 final revision 400；**行锁幂等** `task_idempotency_key` + verbatim 守卫；修复 JOIN 62 列单次 Scan）
- **P3f-e 分集删除文件清理**：级联删除事务内 `delete_files` 门控收集 clean/shared → 提交后 `storage.Remove` 真删对象 → `FinalizeEpisodeDeleteFileCleanupTx` 删文件行 + 操作日志 `detail.file_cleanup` 回填最终计数（修复原 `shared_retained` 错用可删数、响应 key 对齐既定契约 `succeeded/failed/shared_retained/pending_retry/failed_objects/shared_objects`）；共享文件保留（`仍被项目级或其他分集数据引用`）
- 测试全绿：`internal/api` file(4) + asset(8) + task_generators(10) + projects_admin 三个新删除清理用例共 **7 个 episode delete 用例**；`go build / vet / test ./...` 通过（MinIO 用例按 `minioAvailable` 探针门控，真实闭环保留）
- 契约：docs/后端/文件与资产域契约.md

## P3g 完成记录：admin 看板 / 通知读端 / Hermes 静态端点（2026-09-03）

> 范围（与负责人确认）：前端样式 100% 不变，补全管理员页面依赖的后端端点。
> 关键决策：`/agents/hermes/health` **静态配置派生**（P4 接入真实 Hermes 探活前 status=unreachable/disabled）；
> `stats.agentRuns` 用 `agent_runs` queued/running 行数作为在途运行数。
> 契约见 [docs/后端/admin与通知域契约.md](后端/admin与通知域契约.md)。

- [x] 通知读端：`GET /workspace/notifications`（本人最近 100 条倒序）+ `POST /workspace/notifications/read-all`
  （`{"updated":N}`）；写路径仍走 P3e 任务事务内 `insertNotification`，读端只读。
- [x] `/admin/overview`：stats 短键（users/projects/tasks/overdue/assets/submissions/agentRuns）+ extended
  `*_count` 别名、`overdue_tasks` Top5（复用 TaskView 56 字段）、`recent_activities` 逐字两条、
  `recent_readings`（script_reading 最近 3 条 + 分集/项目信息）。
- [x] `/admin/probe`：27 张表行数 counts + `recent_projects`(5) / `recent_agent_runs`(8) / `recent_operation_logs`(8)。
- [x] `/agents/hermes/config` + `/agents/hermes/health`（无鉴权对齐既定契约；config 双通道扩展
  `CINEFORGE_HERMES_{ENABLED,HEALTH_URL,MODEL,DASHBOARD_URL,WEBUI_URL,API_KEY}`；空 dashboard/webui 按域名填充）。
- [x] 契约测试全绿：`internal/api/admin_contract_test.go` 5 个用例（通知读写、overview 形状+403、
  overdue+readings、probe 27 表、hermes enabled/disabled 分支）；`go build / vet / test ./...` 通过。
- [x] P4 已接回：agents 定义/stage-map/agent-jobs/feedback/training-samples/materialize 落地 + Hermes 真实探活（见 P4 完成记录）。

## P4 完成记录：agent 接入（2026-09-01 立项，09-03 闭环）

> 关键决策（与负责人确认）：
> Q1 执行引擎 = **Python `agent/` 子模块（推荐）**——Go 经 `agent-client` 把已落库的运行投递给
> `bridge.py`（Celery `task_id=run_id` 幂等），Python 侧完成既定执行闭包（LangGraph→Hermes→归一化→物化）。
> Q2 P4 范围 = **前端硬契约全落地（推荐）**——五步真实执行、SSE run-events、task prompt-jobs、
> retry-distribution、materialize、project-query、非正式 agent jobs、内置真实探针全部落地；
> `/agents` definitions/stage-map/`/agent-jobs`/feedback/training-samples 按既定契约实现精简集。
> 契约见 [docs/后端/agent域契约.md](后端/agent域契约.md)；prompt-jobs 见 [docs/后端/任务域契约.md](后端/任务域契约.md)。

- **P4a/b agent 子模块**：`agent/` 按既定执行契约实现（Python 3.11/uv；config.py 以
  `CINEFORGE_*` env 映射、`db/base.py` 统一连 `cineforge` 库且无 create_all/ALTER、requirements 按需
  裁剪无 alembic/grpcio/PyJWT）；`bridge.py` `:8091`（GET /health + POST /api/runs/{run_id}/enqueue）；
  Celery `execute_agent_run`（materialize 钩子在 exec 后）。Go 侧 `internal/agentclient`（enqueue/health）。
- **P4c agent-client 接线**：运行落库（`saveRunningRun` 无 op-log）→ EnqueueRun → 入队失败
  run 落 `failed` 但仍按 202/200 返回（对齐 `submit_background`）。
- **P4d SSE run-events**：`GET /projects/{id}/run-events`——快照形状逐字段对齐既定契约
  `_project_run_snapshot`（AgentRunSummaryRead/WorkflowRunSummaryRead，episode/version 来自 summary_json）；
  Redis 不可用 → 503 + Retry-After + polling fallback；401/404/403 逐字。契约测试
  `run_events_contract_test.go`（`disconnectRedis` 把测试 Redis 指向死端口，不依赖开发机 6379）。
- **P4e agent 域**：agents definitions/stage-map/`{agent_type}`/hermes config+**health 真实探活**
  （timeout=min(hermes_timeout_seconds,8s)，health→/models 探测链）；`POST /agent-jobs`
  （director/admin 202，未注册类型 404 `Agent not found`）；`POST /projects/{pid}/agents/{type}/jobs`
  （400 stage gate + pending 去重）；materialize（409 FORMAL gate/400 类型/403/404 + relation_check/
  content_compliance_review 确定性分支 + 幂等 training sample）；retry-distribution（404/403/409 逐字）；
  `POST /projects/{pid}/query-agent`（本地向量 `token_hash_v1`，非导演可见任务过滤）；
  feedback/training-samples（POST 201 / GET 列表，403 `Training sample permission denied`）。
- **P4e-6 task prompt-jobs**：`POST /tasks/{task_id}/prompt-jobs?step=keyframe|video` → 202 AgentRunRead。
  守卫阶梯/类型→Agent/技能映射/input 契约/`context_fingerprint`/生产上下文引用/keyframe_reference/op-log
  （`agent_run_created` detail 含 task_id）全部对齐既定 prompt-jobs 契约；
  实现 `service/tasks_prompt_jobs.go` + `repository/tasks_prompt_jobs.go` + `service/task_prompt_context.go`
  + `productionbrief`（ResolvedProductionBrief.v1 / BuildAgentExecutionContext）。
- **P4e-7 接线验证**：`p4e_routes_test.go` 路由注册守卫（9 条 P4e 路径共存不 panic）+ 全量契约测试。
- 测试全绿：`internal/api` run_events(3) + task_prompt_jobs(4) + p4e_routes(1) 等全量用例；
  `go build / vet / test ./...` 通过。
- [ ] 待补充：真实 Hermes 联调冒烟（本地无 hermes 时 health 走 unreachable 兜底，P5/P6 部署环境验证）。

## 验收标准

任意智能体从根 README 或任一仓库的 AGENTS.md 进入，都能读到全貌（方案 / 规范 / 进度）。